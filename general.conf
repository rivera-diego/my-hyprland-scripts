# --- CONFIGURACIÓN GENERAL ---
general {
    gaps_in = 3
    gaps_out =5
    border_size = 2

    # Layout activo (opciones: master, dwindle, scrolling)
    layout = scrolling

    # Tearing
    allow_tearing = false
}
